import 'package:flutter/material.dart';

class CardProductScreen extends StatelessWidget {
  const CardProductScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Card Product Screen'),
      ),
    );
  }
}
